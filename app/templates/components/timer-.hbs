<div class="timer">
  <div class="time">{{displayTime}}</div>
  <button {{action "startTimer"}}>Start</button>
  <button {{action "pauseTimer"}}>Pause</button>
  <button {{action "stopTimer"}}>Stop</button>
</div>

{{yield}}
